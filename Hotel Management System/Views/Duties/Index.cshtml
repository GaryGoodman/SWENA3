@model IEnumerable<Hotel_Management_System.Models.Duty>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Duty_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Staff_ID)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Duty_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Staff_ID)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Duty_ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.Duty_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Duty_ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
